{% extends 'base.html.twig' %}

{% block title %}Hello InsciptionController!{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{form_label(form.email)}}
        {{form_widget(form.email)}}
        {{form_rest(form)}}
    {{ form_end(form) }}
{% endblock %}
